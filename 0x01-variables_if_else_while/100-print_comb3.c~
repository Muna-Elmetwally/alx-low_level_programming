#include <stdio.h>

/**
 * main - prints all possible different combinations of two digits
 * Return: ALways 0 (Success)
 */
int main(void)
{
int n, m;

{

for (m = 0; m =< 9; m++)
for (n = 0; n < m; n++)
{
  if (m > n)
{
putchar('0' + n);
putchar('0' + m);
       
{
putchar(',');
putchar(' ');
}
}
}
}
putchar('\n');
return (0);
}
